# n8n MCP connection (used by .cursor/mcp.json after substitution)
# Get these from n8n: Settings → Instance-level MCP → Connection details
N8N_MCP_URL=https://your-instance.n8n.cloud/mcp-server/http
N8N_MCP_TOKEN=your-mcp-access-token
